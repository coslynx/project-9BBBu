class Song {
  constructor(title, artist, album, downloadURL) {
    this.title = title;
    this.artist = artist;
    this.album = album;
    this.downloadURL = downloadURL;
  }
}

module.exports = Song;